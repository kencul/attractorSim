# attractorSim
A simulation of gravitational forces with PyGame

## Setup

Install dependencies:
```bash
pip install -r requirements.txt
```

## Usage

Run the script to start the simulation:
```bash
python src/main.py
```